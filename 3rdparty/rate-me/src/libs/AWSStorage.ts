export class AWSStorage {
  constructor() {
    // super loud and slow constructor
  }

  public store(fileContent: string): string {
    //generate path
    const filePath = 'some-random-path';
    //store file
    //return destination path
    return filePath;
  }
}
